name: Test on Multiple Platforms

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-12, macos-14, ubuntu-latest, windows-latest]
      fail-fast: false
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up JDK
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'  # adjust to your Java version
    
    - name: Make gradlew executable (Unix)
      if: runner.os != 'Windows'
      run: chmod +x gradlew
    
    - name: Run tests
      run: ./gradlew clean jvmTest publishToMavenLocal